# Wallet private key used ONLY for deploying the contract (server-side, never shipped to browser)
DEPLOYER_PRIVATE_KEY=

# After deploy, paste the FortressArena address here so the frontend can find it
NEXT_PUBLIC_ARENA_ADDRESS=
